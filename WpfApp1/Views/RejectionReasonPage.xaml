<Page x:Class="WpfApp1.Views.RejectionReasonPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Powód odrzucenia karty projektu">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Podaj powód odrzucenia i sugestie poprawek dla klienta" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBox x:Name="ReasonTextBox" Grid.Row="1" 
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 BorderThickness="1" Padding="5"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="CancelButton" Content="Anuluj" Width="100" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button x:Name="SendButton" Content="Wyślij" Width="100" IsDefault="True" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Page>